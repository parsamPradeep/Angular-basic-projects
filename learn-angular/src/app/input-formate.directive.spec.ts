import { InputFormateDirective } from './input-formate.directive';

describe('InputFormateDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormateDirective();
    expect(directive).toBeTruthy();
  });
});
